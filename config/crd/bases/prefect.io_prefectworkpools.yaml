---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: prefectworkpools.prefect.io
spec:
  group: prefect.io
  names:
    kind: PrefectWorkPool
    listKind: PrefectWorkPoolList
    plural: prefectworkpools
    singular: prefectworkpool
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PrefectWorkPool is the Schema for the prefectworkpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrefectWorkPoolSpec defines the desired state of PrefectWorkPool
            properties:
              server:
                description: Server defines which Prefect Server to connect to
                properties:
                  name:
                    description: Name is the name of the Prefect Server in the given
                      namespace
                    type: string
                  namespace:
                    description: Namespace is the namespace where the Prefect Server
                      is running
                    type: string
                type: object
              version:
                description: Version defines the version of Prefect the Work Pool
                  will run
                type: string
              workers:
                description: Workers defines the number of workers to run in the Work
                  Pool
                format: int32
                type: integer
            type: object
          status:
            description: PrefectWorkPoolStatus defines the observed state of PrefectWorkPool
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
