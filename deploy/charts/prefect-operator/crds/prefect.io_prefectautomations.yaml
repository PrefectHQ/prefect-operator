---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: prefectautomations.prefect.io
spec:
  group: prefect.io
  names:
    kind: PrefectAutomation
    listKind: PrefectAutomationList
    plural: prefectautomations
    shortNames:
    - pa
    singular: prefectautomation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Whether this Prefect automation is ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: The Prefect automation ID
      jsonPath: .status.id
      name: ID
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: PrefectAutomation is the Schema for the prefectautomations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PrefectAutomationSpec defines the desired state of a PrefectAutomation.
              It mirrors the options of the Prefect Terraform provider's prefect_automation
              resource so automations can be managed declaratively via the operator.
            properties:
              actions:
                description: Actions to perform when the automation is triggered
                items:
                  description: |-
                    PrefectAutomationAction matches Prefect's action union. `type` selects the
                    action; the remaining fields apply to the relevant action types.
                  properties:
                    automationId:
                      description: AutomationID targets an automation (pause/resume-automation)
                      type: string
                    blockDocumentId:
                      description: BlockDocumentID targets a block document (send-notification/call-webhook)
                      type: string
                    body:
                      description: Body is the notification body
                      type: string
                    deploymentId:
                      description: |-
                        DeploymentID targets a deployment (run/pause/resume-deployment) by its
                        Prefect UUID.
                      type: string
                    deploymentName:
                      description: |-
                        DeploymentName targets a deployment (run/pause/resume-deployment) by its
                        Prefect deployment name; the operator resolves it to a deployment ID at
                        reconcile time (so automations can reference deployments declaratively,
                        without knowing the runtime-assigned UUID). Mutually exclusive with
                        DeploymentID. If the named deployment does not exist yet, reconciliation
                        requeues until it does.
                      type: string
                    jobVariables:
                      description: JobVariables is a JSON object of job variables
                        for run-deployment
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    message:
                      description: Message is the state message (change-flow-run-state)
                        or notification body
                      type: string
                    name:
                      description: Name is the state name for change-flow-run-state
                      type: string
                    parameters:
                      description: Parameters is a JSON object of parameters for run-deployment
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    payload:
                      description: Payload is the webhook payload (call-webhook)
                      type: string
                    scheduleId:
                      description: ScheduleID targets a schedule (pause/resume-schedule-for-flow-run)
                      type: string
                    source:
                      description: |-
                        Source selects whether the target is "selected" (explicit ID) or "inferred"
                        from the triggering event.
                      enum:
                      - selected
                      - inferred
                      type: string
                    state:
                      description: State is the target state for change-flow-run-state
                      type: string
                    subject:
                      description: Subject is the notification subject
                      type: string
                    type:
                      description: Type is the action to perform.
                      enum:
                      - do-nothing
                      - run-deployment
                      - pause-deployment
                      - resume-deployment
                      - cancel-flow-run
                      - change-flow-run-state
                      - suspend-flow-run
                      - resume-flow-run
                      - pause-work-queue
                      - resume-work-queue
                      - pause-work-pool
                      - resume-work-pool
                      - pause-automation
                      - resume-automation
                      - send-notification
                      - call-webhook
                      - declare-incident
                      - pause-schedule-for-flow-run
                      - resume-schedule-for-flow-run
                      type: string
                    workPoolId:
                      description: WorkPoolID targets a work pool (pause/resume-work-pool)
                      type: string
                    workQueueId:
                      description: WorkQueueID targets a work queue (pause/resume-work-queue)
                      type: string
                  required:
                  - type
                  type: object
                type: array
              actionsOnResolve:
                description: ActionsOnResolve are actions executed when the trigger
                  resolves
                items:
                  description: |-
                    PrefectAutomationAction matches Prefect's action union. `type` selects the
                    action; the remaining fields apply to the relevant action types.
                  properties:
                    automationId:
                      description: AutomationID targets an automation (pause/resume-automation)
                      type: string
                    blockDocumentId:
                      description: BlockDocumentID targets a block document (send-notification/call-webhook)
                      type: string
                    body:
                      description: Body is the notification body
                      type: string
                    deploymentId:
                      description: |-
                        DeploymentID targets a deployment (run/pause/resume-deployment) by its
                        Prefect UUID.
                      type: string
                    deploymentName:
                      description: |-
                        DeploymentName targets a deployment (run/pause/resume-deployment) by its
                        Prefect deployment name; the operator resolves it to a deployment ID at
                        reconcile time (so automations can reference deployments declaratively,
                        without knowing the runtime-assigned UUID). Mutually exclusive with
                        DeploymentID. If the named deployment does not exist yet, reconciliation
                        requeues until it does.
                      type: string
                    jobVariables:
                      description: JobVariables is a JSON object of job variables
                        for run-deployment
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    message:
                      description: Message is the state message (change-flow-run-state)
                        or notification body
                      type: string
                    name:
                      description: Name is the state name for change-flow-run-state
                      type: string
                    parameters:
                      description: Parameters is a JSON object of parameters for run-deployment
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    payload:
                      description: Payload is the webhook payload (call-webhook)
                      type: string
                    scheduleId:
                      description: ScheduleID targets a schedule (pause/resume-schedule-for-flow-run)
                      type: string
                    source:
                      description: |-
                        Source selects whether the target is "selected" (explicit ID) or "inferred"
                        from the triggering event.
                      enum:
                      - selected
                      - inferred
                      type: string
                    state:
                      description: State is the target state for change-flow-run-state
                      type: string
                    subject:
                      description: Subject is the notification subject
                      type: string
                    type:
                      description: Type is the action to perform.
                      enum:
                      - do-nothing
                      - run-deployment
                      - pause-deployment
                      - resume-deployment
                      - cancel-flow-run
                      - change-flow-run-state
                      - suspend-flow-run
                      - resume-flow-run
                      - pause-work-queue
                      - resume-work-queue
                      - pause-work-pool
                      - resume-work-pool
                      - pause-automation
                      - resume-automation
                      - send-notification
                      - call-webhook
                      - declare-incident
                      - pause-schedule-for-flow-run
                      - resume-schedule-for-flow-run
                      type: string
                    workPoolId:
                      description: WorkPoolID targets a work pool (pause/resume-work-pool)
                      type: string
                    workQueueId:
                      description: WorkQueueID targets a work queue (pause/resume-work-queue)
                      type: string
                  required:
                  - type
                  type: object
                type: array
              actionsOnTrigger:
                description: ActionsOnTrigger are actions executed when the trigger
                  fires
                items:
                  description: |-
                    PrefectAutomationAction matches Prefect's action union. `type` selects the
                    action; the remaining fields apply to the relevant action types.
                  properties:
                    automationId:
                      description: AutomationID targets an automation (pause/resume-automation)
                      type: string
                    blockDocumentId:
                      description: BlockDocumentID targets a block document (send-notification/call-webhook)
                      type: string
                    body:
                      description: Body is the notification body
                      type: string
                    deploymentId:
                      description: |-
                        DeploymentID targets a deployment (run/pause/resume-deployment) by its
                        Prefect UUID.
                      type: string
                    deploymentName:
                      description: |-
                        DeploymentName targets a deployment (run/pause/resume-deployment) by its
                        Prefect deployment name; the operator resolves it to a deployment ID at
                        reconcile time (so automations can reference deployments declaratively,
                        without knowing the runtime-assigned UUID). Mutually exclusive with
                        DeploymentID. If the named deployment does not exist yet, reconciliation
                        requeues until it does.
                      type: string
                    jobVariables:
                      description: JobVariables is a JSON object of job variables
                        for run-deployment
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    message:
                      description: Message is the state message (change-flow-run-state)
                        or notification body
                      type: string
                    name:
                      description: Name is the state name for change-flow-run-state
                      type: string
                    parameters:
                      description: Parameters is a JSON object of parameters for run-deployment
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    payload:
                      description: Payload is the webhook payload (call-webhook)
                      type: string
                    scheduleId:
                      description: ScheduleID targets a schedule (pause/resume-schedule-for-flow-run)
                      type: string
                    source:
                      description: |-
                        Source selects whether the target is "selected" (explicit ID) or "inferred"
                        from the triggering event.
                      enum:
                      - selected
                      - inferred
                      type: string
                    state:
                      description: State is the target state for change-flow-run-state
                      type: string
                    subject:
                      description: Subject is the notification subject
                      type: string
                    type:
                      description: Type is the action to perform.
                      enum:
                      - do-nothing
                      - run-deployment
                      - pause-deployment
                      - resume-deployment
                      - cancel-flow-run
                      - change-flow-run-state
                      - suspend-flow-run
                      - resume-flow-run
                      - pause-work-queue
                      - resume-work-queue
                      - pause-work-pool
                      - resume-work-pool
                      - pause-automation
                      - resume-automation
                      - send-notification
                      - call-webhook
                      - declare-incident
                      - pause-schedule-for-flow-run
                      - resume-schedule-for-flow-run
                      type: string
                    workPoolId:
                      description: WorkPoolID targets a work pool (pause/resume-work-pool)
                      type: string
                    workQueueId:
                      description: WorkQueueID targets a work queue (pause/resume-work-queue)
                      type: string
                  required:
                  - type
                  type: object
                type: array
              description:
                description: Description is human-readable documentation for the automation
                type: string
              enabled:
                description: Enabled activates or deactivates the automation
                type: boolean
              interval:
                description: |-
                  Interval is how often to re-check this automation against the Prefect API
                  to correct out-of-band drift (edits or deletes made directly in Prefect).
                  Defaults to the operator's --default-resync-interval when unset. Values
                  below 10s are clamped.
                type: string
              name:
                description: Name of the automation
                type: string
              server:
                description: Server configuration for connecting to the Prefect API
                properties:
                  accountId:
                    description: AccountID is the ID of the account to use to connect
                      to Prefect Cloud
                    type: string
                  apiKey:
                    description: APIKey is the API key to use to connect to a remote
                      Prefect Server
                    properties:
                      value:
                        description: Value is the literal value of the API key
                        type: string
                      valueFrom:
                        description: ValueFrom is a reference to a secret containing
                          the API key
                        properties:
                          configMapKeyRef:
                            description: Selects a key of a ConfigMap.
                            properties:
                              key:
                                description: The key to select.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the ConfigMap or its
                                  key must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          fieldRef:
                            description: |-
                              Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                              spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                            properties:
                              apiVersion:
                                description: Version of the schema the FieldPath is
                                  written in terms of, defaults to "v1".
                                type: string
                              fieldPath:
                                description: Path of the field to select in the specified
                                  API version.
                                type: string
                            required:
                            - fieldPath
                            type: object
                            x-kubernetes-map-type: atomic
                          fileKeyRef:
                            description: |-
                              FileKeyRef selects a key of the env file.
                              Requires the EnvFiles feature gate to be enabled.
                            properties:
                              key:
                                description: |-
                                  The key within the env file. An invalid key will prevent the pod from starting.
                                  The keys defined within a source may consist of any printable ASCII characters except '='.
                                  During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                type: string
                              optional:
                                default: false
                                description: |-
                                  Specify whether the file or its key must be defined. If the file or key
                                  does not exist, then the env var is not published.
                                  If optional is set to true and the specified key does not exist,
                                  the environment variable will not be set in the Pod's containers.

                                  If optional is set to false and the specified key does not exist,
                                  an error will be returned during Pod creation.
                                type: boolean
                              path:
                                description: |-
                                  The path within the volume from which to select the file.
                                  Must be relative and may not contain the '..' path or start with '..'.
                                type: string
                              volumeName:
                                description: The name of the volume mount containing
                                  the env file.
                                type: string
                            required:
                            - key
                            - path
                            - volumeName
                            type: object
                            x-kubernetes-map-type: atomic
                          resourceFieldRef:
                            description: |-
                              Selects a resource of the container: only resources limits and requests
                              (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                            properties:
                              containerName:
                                description: 'Container name: required for volumes,
                                  optional for env vars'
                                type: string
                              divisor:
                                anyOf:
                                - type: integer
                                - type: string
                                description: Specifies the output format of the exposed
                                  resources, defaults to "1"
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              resource:
                                description: 'Required: resource to select'
                                type: string
                            required:
                            - resource
                            type: object
                            x-kubernetes-map-type: atomic
                          secretKeyRef:
                            description: Selects a key of a secret in the pod's namespace
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                    type: object
                  name:
                    description: Name is the name of the in-cluster Prefect Server
                      in the given namespace
                    type: string
                  namespace:
                    description: Namespace is the namespace where the in-cluster Prefect
                      Server is running
                    type: string
                  remoteApiUrl:
                    description: RemoteAPIURL is the API URL for the remote Prefect
                      Server. Set if using with an external Prefect Server or Prefect
                      Cloud
                    type: string
                  workspaceId:
                    description: WorkspaceID is the ID of the workspace to use to
                      connect to Prefect Cloud
                    type: string
                type: object
              trigger:
                description: |-
                  Trigger is the criteria for which events this automation covers.
                  Exactly one of event, metric, compound, or sequence must be set.
                properties:
                  compound:
                    description: Compound trigger fires when a number of child triggers
                      fire within a window
                    properties:
                      require:
                        description: 'Require is how many child triggers must fire:
                          "any", "all", or a number'
                        type: string
                      triggers:
                        description: Triggers are the child triggers (event or metric)
                        items:
                          description: |-
                            PrefectChildTrigger is a trigger nested inside a compound or sequence trigger.
                            Prefect only allows event or metric triggers as children (not further
                            compound/sequence nesting), which also keeps the CRD schema non-recursive.
                            Exactly one of event or metric must be set.
                          properties:
                            event:
                              description: Event trigger
                              properties:
                                after:
                                  description: After is the set of event names that
                                    must precede the expected events
                                  items:
                                    type: string
                                  type: array
                                expect:
                                  description: Expect is the set of event names the
                                    trigger expects to see
                                  items:
                                    type: string
                                  type: array
                                forEach:
                                  description: ForEach groups events by the given
                                    resource fields
                                  items:
                                    type: string
                                  type: array
                                match:
                                  description: Match is a JSON object matching resources
                                    by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                matchRelated:
                                  description: MatchRelated is a JSON object matching
                                    related resources by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                posture:
                                  description: Posture is the trigger posture.
                                  enum:
                                  - Reactive
                                  - Proactive
                                  type: string
                                threshold:
                                  description: Threshold is the number of events required
                                    to fire the trigger
                                  type: integer
                                within:
                                  description: Within is the time window in seconds
                                  type: integer
                              required:
                              - posture
                              type: object
                            metric:
                              description: Metric trigger
                              properties:
                                match:
                                  description: Match is a JSON object matching resources
                                    by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                matchRelated:
                                  description: MatchRelated is a JSON object matching
                                    related resources by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                metric:
                                  description: Metric is the metric query definition
                                  properties:
                                    firingFor:
                                      description: FiringFor is the duration in seconds
                                        the query must breach/resolve continuously
                                      type: integer
                                    name:
                                      description: Name is the metric to query
                                      type: string
                                    operator:
                                      description: Operator is the comparative operator
                                        used to evaluate the query result
                                      enum:
                                      - <
                                      - <=
                                      - '>'
                                      - '>='
                                      type: string
                                    range:
                                      description: Range is the lookback duration
                                        in seconds for the metric query
                                      type: integer
                                    threshold:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Threshold is the value the metric
                                        is compared against (may be fractional)
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                  required:
                                  - firingFor
                                  - name
                                  - operator
                                  - range
                                  - threshold
                                  type: object
                              required:
                              - metric
                              type: object
                          type: object
                        type: array
                      within:
                        description: Within is the time window in seconds
                        type: integer
                    required:
                    - require
                    - triggers
                    type: object
                  event:
                    description: Event trigger reacts to (or waits for the absence
                      of) events
                    properties:
                      after:
                        description: After is the set of event names that must precede
                          the expected events
                        items:
                          type: string
                        type: array
                      expect:
                        description: Expect is the set of event names the trigger
                          expects to see
                        items:
                          type: string
                        type: array
                      forEach:
                        description: ForEach groups events by the given resource fields
                        items:
                          type: string
                        type: array
                      match:
                        description: Match is a JSON object matching resources by
                          label
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      matchRelated:
                        description: MatchRelated is a JSON object matching related
                          resources by label
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      posture:
                        description: Posture is the trigger posture.
                        enum:
                        - Reactive
                        - Proactive
                        type: string
                      threshold:
                        description: Threshold is the number of events required to
                          fire the trigger
                        type: integer
                      within:
                        description: Within is the time window in seconds
                        type: integer
                    required:
                    - posture
                    type: object
                  metric:
                    description: Metric trigger fires when a metric crosses a threshold
                    properties:
                      match:
                        description: Match is a JSON object matching resources by
                          label
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      matchRelated:
                        description: MatchRelated is a JSON object matching related
                          resources by label
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      metric:
                        description: Metric is the metric query definition
                        properties:
                          firingFor:
                            description: FiringFor is the duration in seconds the
                              query must breach/resolve continuously
                            type: integer
                          name:
                            description: Name is the metric to query
                            type: string
                          operator:
                            description: Operator is the comparative operator used
                              to evaluate the query result
                            enum:
                            - <
                            - <=
                            - '>'
                            - '>='
                            type: string
                          range:
                            description: Range is the lookback duration in seconds
                              for the metric query
                            type: integer
                          threshold:
                            anyOf:
                            - type: integer
                            - type: string
                            description: Threshold is the value the metric is compared
                              against (may be fractional)
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                        required:
                        - firingFor
                        - name
                        - operator
                        - range
                        - threshold
                        type: object
                    required:
                    - metric
                    type: object
                  sequence:
                    description: Sequence trigger fires when child triggers fire in
                      order within a window
                    properties:
                      triggers:
                        description: Triggers are the child triggers, which must fire
                          in order
                        items:
                          description: |-
                            PrefectChildTrigger is a trigger nested inside a compound or sequence trigger.
                            Prefect only allows event or metric triggers as children (not further
                            compound/sequence nesting), which also keeps the CRD schema non-recursive.
                            Exactly one of event or metric must be set.
                          properties:
                            event:
                              description: Event trigger
                              properties:
                                after:
                                  description: After is the set of event names that
                                    must precede the expected events
                                  items:
                                    type: string
                                  type: array
                                expect:
                                  description: Expect is the set of event names the
                                    trigger expects to see
                                  items:
                                    type: string
                                  type: array
                                forEach:
                                  description: ForEach groups events by the given
                                    resource fields
                                  items:
                                    type: string
                                  type: array
                                match:
                                  description: Match is a JSON object matching resources
                                    by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                matchRelated:
                                  description: MatchRelated is a JSON object matching
                                    related resources by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                posture:
                                  description: Posture is the trigger posture.
                                  enum:
                                  - Reactive
                                  - Proactive
                                  type: string
                                threshold:
                                  description: Threshold is the number of events required
                                    to fire the trigger
                                  type: integer
                                within:
                                  description: Within is the time window in seconds
                                  type: integer
                              required:
                              - posture
                              type: object
                            metric:
                              description: Metric trigger
                              properties:
                                match:
                                  description: Match is a JSON object matching resources
                                    by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                matchRelated:
                                  description: MatchRelated is a JSON object matching
                                    related resources by label
                                  type: object
                                  x-kubernetes-preserve-unknown-fields: true
                                metric:
                                  description: Metric is the metric query definition
                                  properties:
                                    firingFor:
                                      description: FiringFor is the duration in seconds
                                        the query must breach/resolve continuously
                                      type: integer
                                    name:
                                      description: Name is the metric to query
                                      type: string
                                    operator:
                                      description: Operator is the comparative operator
                                        used to evaluate the query result
                                      enum:
                                      - <
                                      - <=
                                      - '>'
                                      - '>='
                                      type: string
                                    range:
                                      description: Range is the lookback duration
                                        in seconds for the metric query
                                      type: integer
                                    threshold:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Threshold is the value the metric
                                        is compared against (may be fractional)
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                  required:
                                  - firingFor
                                  - name
                                  - operator
                                  - range
                                  - threshold
                                  type: object
                              required:
                              - metric
                              type: object
                          type: object
                        type: array
                      within:
                        description: Within is the time window in seconds
                        type: integer
                    required:
                    - triggers
                    type: object
                type: object
            required:
            - name
            - server
            - trigger
            type: object
          status:
            description: PrefectAutomationStatus defines the observed state of a PrefectAutomation.
            properties:
              conditions:
                description: Conditions store the status conditions of the PrefectAutomation
                  instances
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              id:
                description: Id is the automation ID from Prefect
                type: string
              lastSyncTime:
                description: LastSyncTime is the last time the automation was synced
                  with Prefect
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration tracks the last processed generation
                format: int64
                type: integer
              ready:
                description: Ready indicates that the automation exists and is configured
                  correctly
                type: boolean
              specHash:
                description: SpecHash tracks changes to the spec to minimize API calls
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
